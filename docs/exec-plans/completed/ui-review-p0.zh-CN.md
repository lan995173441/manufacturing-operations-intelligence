# UI 评审 — P0 修正

状态：已完成；浏览器视觉验证存在下述限制。英文对照：[ui-review-p0.md](ui-review-p0.md)。

范围：检查 Streamlit 五个区域，以中英文评审文档排序十项 UX 改进，仅在 UI 实施 P0。业务规则、服务、持久化、依赖和公式均不修改。

依据：AGENTS.md、ARCHITECTURE.md、PRD.md、现有 UI、服务结果契约及仪表盘测试。UI_SPEC.md 缺失；本次评审不虚构已获批 UI 规范。V0 文档的历史状态不代表当前实现状态。

1. 检查正常、空数据、异常检查不完整及导入反馈状态；条件允许时保存浏览器证据。
2. P0-01：区分未配置、不支持、无数据、检查不完整与检查完成且无异常；显示跳过检查的原因。
3. P0-02：说明演示数据/上传激活会替换全部四类数据，仅在已有活动数据时要求在界面确认知悉。
4. P0-03：显示校验数量、完整来源位置及原始/规范化值；规范化详情默认折叠；主提示不暴露原始持久化异常。
5. 为上述用户可见行为增加回归测试，运行完整测试与 Ruff，并目视检查修改状态。
6. 记录证据，验证下层文件哈希未变，将本计划及英文版移至 completed。

假设：P0 指可能误导运营判断、意外替换数据、或无法审计/修正导入的问题。P1/P2 仅建议。确认知悉是 UI 交互，不改变原子替换或校验策略。不编造制造业默认阈值。

## 完成证据

- [UI_REVIEW](../../design/UI_REVIEW.zh-CN.md) 记录十项排序：UX-01–03 已实施，UX-04–10 暂缓。
- 仪表盘回归：12 项通过，13.53 秒（新增 10 项）。完整测试：103 项通过，17.76 秒。Ruff 全部通过。
- AppTest 遍历五个区域，异常数为零。下层代码 SHA-256 哈希未变。
- 本地 HTTP 返回 200。沙箱限制进程通信端口，阻止 Chrome 启动，未能完成截图/像素级验证；中英文评审均记录此限制。
- 应用代码仅修改 `src/manufacturing_operations_intelligence/ui/app.py`；在 `tests/test_dashboard.py` 增加测试；新增中英文评审和执行计划。没有新增依赖或业务行为。
