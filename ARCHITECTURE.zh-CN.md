# Manufacturing Operations Intelligence（制造运营智能分析）——架构

日期：2026-09-15  
状态：待审阅的设计建议；用户提供的技术约束必须遵守。本文不授权实施。  
英文对应文档：[ARCHITECTURE.md](ARCHITECTURE.md)

QA 状态（2026-09-17）：本文是原始设计记录。后续明确的功能任务已实现本地 MVP；下文关于未来文档缺失或尚未实施的语句描述的是设计当时状态。QA 发现与剩余决策记录于 [qa-hardening](docs/exec-plans/active/qa-hardening.zh-CN.md)。

## 1. 依据、决策及未决需求

已阅读 [AGENTS V0](AGENTS.zh-CN.md)、[项目章程](docs/product/PROJECT_CHARTER.zh-CN.md)、[PRD](docs/product/PRD.zh-CN.md) 和 [用户故事](docs/product/USER_STORIES.zh-CN.md)。设计覆盖 PRD MUST 能力 F01–F12，并为 F13 提供隔离的可选扩展，不将 S01/S02/C01 升为必需功能。

用户现已指定 Python、Streamlit、Pandas、Plotly、SQLite、OpenPyXL 和 ReportLab。这解决了此前未定的技术栈和数据库技术，不决定持久化数据的业务含义。下述架构是这些技术的组织建议，不代表批准全部 PRD 假设。

数据契约、KPI 定义、UI 规范、测试计划和验收标准仍是缺失的未来权威来源。字段定义、业务键、公式、阈值、库存语义、筛选、版式和发布门槛由这些文档解决。本文不定义制造公式、数据表 DDL 或数值异常阈值。

| 现有差异 / 开放决策 | 架构处理 |
| --- | --- |
| 章程仅 Excel 单表；后续要求 Excel/CSV 四域 | 提供两种格式读取器；具体文件/工作簿包装仍由 D01/D10 决定。 |
| 章程排除库存和 PDF；后续要求包含 | 明确遵循后续请求，包含 Inventory 和 PDF 边界；不声称章程已对齐。 |
| 一个仪表板与五个区域 | 单一应用中的五个轻量视图，不建立独立应用或角色系统。 |
| PRD 称数据库技术待定 | SQLite 现为用户明确约束；替换/合并策略 D05 仍开放。 |
| PRD/AGENTS 清单称架构缺失 | V0 历史清单早于本产物。架构现已存在，本次不升级 V1 或广泛重写文档。 |
| D02–D06/D08：粒度、完整性、库存、导入身份、规则及筛选 | 隔离在数据校验/领域逻辑；实施前正式记录，不自行选择默认值。 |
| D07：可选 AI 交付 | 无 AI 时核心完整；供应商选择和交付优先级独立于此集成边界。 |
| D09/D11/D14/D15：HTML、工时、性能、报告版式 | 仅设计必需 Excel/PDF；HTML 保留待决。不宣称旧工时/性能估算或草案工作表/页数限制已获批。 |

相关决策：[ADR-001](docs/decisions/ADR-001-tech-stack.zh-CN.md)、[ADR-002](docs/decisions/ADR-002-database.zh-CN.md)、[ADR-003](docs/decisions/ADR-003-ai-boundary.zh-CN.md)。

## 2. 系统上下文与运行方式

一名分析师通过本地浏览器操作一个 Streamlit Python 进程。合成 Excel/CSV 文件输入应用；规范化记录及导入来源信息持久化到一个本地 SQLite 数据库。浏览器展示分析并下载生成的 Excel/PDF。仅在明确请求可选摘要时访问外部模型。

不设置独立 Web API、前端服务器、数据库服务器、任务队列、调度器、认证或云平台。操作同步且由用户触发。SQLite 保存持久状态；Streamlit 会话状态保存临时选择和视图结果，不作为导入记录的唯一副本。会话状态关联用户会话，因此重启恢复依赖持久化。[Streamlit 会话状态文档](https://docs.streamlit.io/develop/concepts/architecture/session-state)

单用户本地运行是章程基线，不代表可安全公开托管或支持团队并发。本地运行限制在本机；超出该环境部署需单独的范围/安全决策。测量前不宣称性能容量。

## 3. ASCII 架构图

```text
                     Analyst / local browser
                              |
 +-------------------------------------------------------------+
 | UI: Streamlit                                                |
 | Overview | Production | Quality | Inventory | Reports        |
 | Plotly displays result tables; no business calculations      |
 +----------------------------+--------------------------------+
                              v
 +-------------------------------------------------------------+
 | Application / Service Layer                                 |
 | import | analyze | export | optional summarize               |
 | Owns workflow, result identity, error translation            |
 +----------------------------+--------------------------------+
                              v
 +-------------------------------------------------------------+
 | Domain Analytics                                            |
 | validate | normalize | select/join | KPIs | rule anomalies    |
 | Pure Python/Pandas functions; structured analytics result    |
 +----------------------------+--------------------------------+
                              v
 +-------------------------------------------------------------+
 | Data / Repository Layer                                     |
 | Excel/CSV readers | one concrete SQLite repository           |
 | Stored normalized records + provenance; no KPI SQL           |
 +----------------------------+--------------------------------+
                              |
                        Local SQLite file

 Service also calls lower data modules directly for I/O.
 The vertical arrows show allowed downward dependency direction,
 not a requirement that pure analytics call the repository.

 Service -> validated result -> Excel renderer (OpenPyXL) -> XLSX
                            +-> PDF renderer (ReportLab) -> PDF
                            +-> optional AI adapter -> external LLM
                                      |
                             labeled draft or fallback
```

报告和 AI 是服务调用的输出适配器，不是额外业务层。上传通过 UI 和服务进入读取器，服务将解析记录交给领域校验。结果向上返回 UI。图中垂直箭头表示允许向下的依赖方向，不要求纯分析函数调用仓库；服务可直接调用下层数据模块执行 I/O。

## 4. 主要模块及职责

以下为建议逻辑模块名，不是本次创建的文件。采用普通模块/函数，仅在职责或可读性需要时拆分文件。

| 模块 / 层 | 职责 | 禁止事项 |
| --- | --- | --- |
| `ui` — UI | 五区域导航、上传控件、筛选、诊断展示、Plotly 图形和下载 | SQL、业务关联、KPI 汇总、异常决策、直接模型调用 |
| `services` — 应用 | 编排导入、分析、报告导出和可选摘要；解析当前数据集身份；返回明确结果 | 重复公式、实现结构定义、绘制 Streamlit 控件 |
| `domain.validation` — 领域 | 按未来数据契约执行结构/值/关联检查，生成带位置的问题 | 读取文件、静默修复运营值、选择未记录规则 |
| `domain.normalization` — 领域 | 已记录的转换，保留来源引用，转换后校验 | 未批准的填补或丢弃无效行 |
| `domain.analytics` — 领域 | 适用选择、关联、KPI、趋势/对比表、库存计算和确定性异常规则 | 依赖 Streamlit、SQL、报告、模型、网络或文件系统 |
| `data.readers` — 数据 | 解码 Excel/CSV 为候选记录，保留源名称/位置及标识符，报告解析失败 | 宣称数据业务有效或计算指标 |
| `data.repository` — 数据 | 单一具体 SQLite 仓库；连接、一致读取、原子写入、来源及结构版本检查 | Streamlit 状态、报告排版、在 SQL 中计算业务 KPI |
| `reporting` — 输出适配器 | OpenPyXL 和 ReportLab 消费同一分析结果，负责展示格式和分页 | 加载数据库记录或重算业务指标 |
| `summaries` — 可选输出适配器 | 基于结果生成确定性文本；通过选定汇总事实请求可选模型；标记结果 | 写入源/数据库数据，或成为分析/报告前提 |

可用少量标准 Python 记录描述校验问题、有效批次、分析结果及摘要结果，放在所属层附近。不构建通用数据传输框架或杂项公共工具包。

### 五个应用区域

- **Overview（F07）：** 展示服务提供的汇总指标、趋势、覆盖和异常概览。
- **Production（F08）：** 展示已提供的计划/实绩对比、生产趋势和带来源异常。
- **Quality（F09）：** 展示已提供的质量指标、趋势和异常证据。
- **Inventory（F10）：** 展示已提供的库存指标与覆盖；精确快照/筛选语义仍由 D04/D08 决定。
- **Reports & Insights（F11/F12，可选 F13）：** 预览明确的报告范围，请求 Excel/PDF，并可选请求有标识的 AI 草稿。

Plotly 接收已计算序列。页面可排列列、设置标签或展示分页；改变指标总体、总量、比率、业务排名或异常归属的操作属于分析层。导航不必对应五个物理页面文件。

## 5. 依赖方向与边界契约

规定的概念层次为 **UI → 应用/服务 → 领域分析 → 数据/仓库**。下层不导入 UI 或服务。适当时上层可直接调用更下层：服务通过数据层执行 I/O，将返回记录传给纯领域函数。领域可以完全不依赖数据层，既避免在 KPI 函数中隐藏仓库调用，也保持依赖仅向下。

| 调用方 | 允许依赖 |
| --- | --- |
| UI | 服务、Streamlit、Plotly、仅展示格式化 |
| 服务 | 领域、具体数据模块、报告、摘要 |
| 领域 | Python 标准库和 Pandas，不依赖 I/O 适配器 |
| 数据 | Python `sqlite3`/文件设施、用于解析的 Pandas/OpenPyXL，不导入上层 |
| 报告 | 标准库、OpenPyXL/ReportLab，仅引用结果记录定义 |
| 摘要 | 标准库、结果定义、可选且隔离的供应商传输 |

边界记录携带数据，而非活动数据库连接或 Streamlit 对象：

- **候选记录：** 解码的源值及位置引用，尚未可信。
- **校验结果 / 有效批次：** 明确问题、允许转换日志、契约版本及接受的规范化记录。仅成功结果可提交。
- **分析请求：** 数据集身份及明确范围。领域按批准定义解析业务选择。
- **分析结果：** 指标、准备好的图表/明细表、异常依据、校验/覆盖状态、源引用、数据集身份、实际筛选及契约/KPI/规则版本。渲染器不编造缺失值。
- **摘要结果：** 草稿/回退/不可用状态、文本及其来源分析结果身份。

这些是概念载荷，不是最终结构。结果按只读处理：UI/渲染器不得修改共享 Pandas 表，需要时使用本地展示副本。不需要序列化协议、通用仓库接口、依赖注入容器或事件总线。

## 6. 数据流与 Streamlit 重跑

1. **接收：** UI 收集文件字节和预期数据域；明确导入操作调用服务。仅上传不替换已接受数据。
2. **解析：** 读取器解码且不丢失源位置。自动类型推断会破坏标识含义时不得依赖它。支持的包装和限制遵循未来数据契约。
3. **校验及转换：** 领域检查候选值，仅执行批准转换，再校验规范化键和关联。按批准契约区分空、无效和零，诊断明确保留无效输入信息。
4. **持久化：** 服务向仓库提交成功批次，在一个事务内提交全部受影响记录及元数据。整批替换是 D01/D03/D05 建议，不是本文定稿的业务规则。
5. **一致读取：** 服务在一致数据库读取中加载规范化记录及身份，计算前关闭连接。不混合不同当前数据集的各域记录。
6. **计算：** 领域针对请求范围生成指标、视图表、覆盖和异常依据，结果记录定义版本和源身份。
7. **展示：** 服务返回结果，五视图渲染对应部分，无数据和不可用状态明确。
8. **导出：** 导出服务核对身份/范围，将同一结果交给两种渲染器。报告范围与页面局部筛选不同，则通过分析服务获取新结果并披露范围，渲染器不自行筛选/重算指标。
9. **可选叙述：** 明确请求后总结当前结构化结果，重新生成报告可附加匹配摘要；报告本身不调用模型。

当前 UI 会话保存临时上传状态、筛选及最近结果。Streamlit 重跑不得自动重复导入、导出或 AI 调用，昂贵副作用需要明确操作。复用/导出前检查结果的数据集及定义版本、实际筛选；不匹配则丢弃缓存视图/摘要并获取新结果。结果创建后若新批次提交，导出必须使用已核实且明确标识的快照，或发布前刷新，不能将旧数值标为新数据集。

初始不使用跨会话缓存、缓存共享数据库连接或工作队列。需要时由服务重新计算分析；“报告不重算”指渲染器不能独立实现指标，不禁止为变化的范围调用分析服务。

## 7. 持久化方案

通过 Python `sqlite3` 使用一个本地 SQLite 文件和一个具体仓库。SQLite 无需单独数据库服务器，符合本地 MVP。必须使用参数化查询和明确控制的事务。[Python sqlite3 文档](https://docs.python.org/3/library/sqlite3.html)

持久化四域规范化记录、解释有效数据所需的来源/校验/转换信息、数据集身份及结构/契约版本元数据。具体表、列、约束和键关联待数据契约定义。不得以 SQLite 宽松的值处理替代领域校验。

对于建议替换策略，在写事务外规范化，在事务内一起替换受影响记录并更新当前身份。任何失败均回滚。宣告导入失败前确认旧数据集仍为当前；若提交结果不确定，重新打开并检查身份，不自动重提批次。重复导入身份/比较语义仍由 D05 决定；事务安全不等于该策略已定。

每项操作使用短生命周期连接，所有路径均关闭。绘图、文件生成和网络调用期间不持有事务。锁/忙错误采用有限等待并返回明确可重试消息。不使用跨 Streamlit 会话的全局连接、服务器数据库、ORM 或连接池。

在一个读取快照下读取全部所需域。仓库 SQL 只负责存取，不负责 KPI 关联、汇总或异常阈值；业务选择和计算属于领域。结构版本不匹配时阻止正常访问并明确提示升级，不静默删除/重建或重新解释已有数据库。初始结构建立及后续小型明确迁移属于未来实施任务，现在不引入迁移框架。

不把派生 KPI 持久化为第二套事实来源，而从已接受记录和版本化定义重算。报告/AI 草稿可保持临时状态；重启保留规范化数据，但需要重算视图结果。原始输入文件不修改；默认不在 SQLite 存档其完整内容或被拒批次。来源信息不等于源文件保留或企业审计承诺。

## 8. 错误处理策略

| 失败 | 负责方与响应 | 状态保证 |
| --- | --- | --- |
| 损坏/不支持文件 | 读取器返回解析错误，服务附加数据域/文件上下文 | 不改变已接受数据 |
| 结构/值/关联失败 | 领域返回结构化问题及可用行/字段依据 | 不静默丢弃行，不激活拒绝批次 |
| 数据库锁/写失败 | 仓库回滚，服务报告可重试或需恢复状态 | 已提交旧数据仍权威；检查不确定结果 |
| 定义或结构缺失/不兼容 | 服务阻断依赖操作，指出缺失来源/版本 | 不猜测规则或自动破坏性重置 |
| 空数据或不可用指标 | 领域返回明确状态和覆盖 | 不编造指标值 |
| 意外分析异常 | 服务明确分析失败，记录安全诊断上下文 | 不将部分结果展示为完整；已接受源数据完整 |
| Excel/PDF 渲染失败 | 导出服务返回对应格式失败 | 无部分下载，数据库及其他已完成产物不变 |
| AI 关闭/失败/响应不可用 | 摘要适配器返回标记回退或不可用 | 核心分析和报告仍可用 |

不得捕获所有异常后用空表继续。预期校验结果与意外失败分开。标准 Python 日志足够，记录操作引用、分类及脱敏诊断上下文。绝不记录 API 密钥、连接秘密、完整上传数据或完整模型载荷。UI 获得有用恢复指引，不展示原始堆栈。

## 9. 报告架构

一个报告服务接受有效分析结果及可选的匹配摘要，把同一组已计算数值、表格、定义、范围和依据传入两个格式函数：

- **Excel：** OpenPyXL 将最终值、标签、单位和来源写入已批准版式，不嵌入业务公式作为第二个计算引擎。源字符串作为字面数据，不作为可执行表格公式。
- **PDF：** ReportLab 使用结果中准备好的序列处理分页、表格、页码和简单矢量图。使用 ReportLab 图表/绘图设施，不导出 Plotly 图片，避免仅为 PDF 增加 Kaleido、浏览器运行时或图像转换流程。ReportLab 提供文档布局和图形设施。[ReportLab 用户指南](https://www.reportlab.com/docs/reportlab-userguide.pdf)

Plotly 用于交互 UI，ReportLab 用于静态报告。同一序列可采用不同展示形式，但渲染器不能改变值。可换行、分页或按获批规则截断明细并披露，不得从原始行计算总量/比率。精确图表、工作表、字体、语言及截断限制仍属于 D15 下 UI_SPEC/验收标准决策。

为该有限本地流程在内存生成完整字节，成功后才提供下载。实施验收前结合文件限制测量内存。不增加报告数据库、调度器、邮件、HTML 导出子系统或产物存储服务。报告从不要求 AI 联网，不修改持久化数据。

## 10. 可选 AI 边界

分析服务在无 AI 时生成权威结构化结果。摘要服务从结果中按白名单构建汇总事实载荷：报告事实标识、展示值/单位、批准对比、异常依据摘要、范围及局限。不向提示词提供原始输入行、文件、数据库访问或凭据。

单个可选供应商适配器可将载荷转为需人工复核的草稿。本次不选供应商/模型/SDK。未来启用时仅在明确请求后加载配置，使用环境秘密，限制超时/输出大小，避免自动循环重试。无工具调用、自主动作或多供应商框架。

从已有结果字段生成确定性管理文本作为离线路径。AI 关闭、缺密钥、供应商错误或输出被拒绝时，返回回退文本或明确的叙述不可用状态，不阻断指标/报告。摘要携带结果身份，数据集、范围或定义变化后失效。

校验响应结构，并在可机械核对处比对事实载荷；拒绝无依据数值，不将其插入报告。此类检查不能证明叙述正确或因果成立，仍须人工复核和代表场景评估。AI 不能更改记录、规则、阈值、计算或合规决策。见 [ADR-003](docs/decisions/ADR-003-ai-boundary.zh-CN.md)。

## 11. 测试架构与 MUST 覆盖

尽可能不启动 Streamlit，直接测试普通函数和服务入口。使用临时文件/数据库及合成样例。初期 Python 标准测试设施足够；框架/lint 命令留待 TEST_PLAN 和 AGENTS V1。本次不创建测试代码。

| 边界 / 需求 | 计划证据 |
| --- | --- |
| 读取器及领域校验，F01–F03 | 等价的受支持 Excel/CSV 样例；损坏文件、缺失/无效字段、标识保留、规范化冲突和来源位置。预期结果来自批准的数据契约。 |
| 仓库及服务，F04 | 临时磁盘 SQLite、关闭/重开恢复、原子提交和注入失败、锁处理、结构不匹配，以及批准的重导/替换行为。重启证据使用磁盘，不只测内存库。 |
| 纯分析，F05–F06 | 独立推导的批准口径样例，缺失/零、分组、库存总体、异常边界及跳过检查。不得由被测代码生成预期值。 |
| UI/服务边界，F07–F10 | 各区域值可追溯结果字段；导航/筛选/空数据检查；重跑不重复导入副作用；页面无公式或数据库调用。 |
| 报告适配器，F11–F12 | 同一结果生成两种格式；OpenPyXL 回读数值及字面文本安全；PDF 视觉核查裁切、分页、标签和事实一致性。报告测试使用给定结果，不读取仓库。 |
| 可选 AI，F13 | 假供应商测试关闭/缺密钥/超时/格式错误/无依据陈述；身份失效及无供应商时核心导出。核心测试不依赖真实模型调用。 |
| 离线集成流程，F01–F12 | 有效输入 → 持久化规范数据 → 指标/异常 → 五区域 → Excel/PDF；无效导入后旧数据仍明确标识且可用。 |
| 依赖规则 | 小型导入/依赖检查或专项审查，确认领域无 UI/I/O 导入、报告无业务计算或仓库调用。不引入架构执行框架。 |

未来 TEST_PLAN 定义程序，ACCEPTANCE_CRITERIA 定义发布门槛。按批准负载测试时间、内存和可读性，不静默采用未决 PRD 耗时目标。不为测试通过改变业务定义。

## 12. 抽象必要性审查

| 建议抽象 | 保留？ | 必要性 / 更简单边界 |
| --- | --- | --- |
| 五个 UI 视图 | 是 | 必需区域，普通函数足够，不是五个服务。 |
| 应用服务 | 是 | 防止 UI 拥有事务、计算和副作用；可从一个模块开始编排。 |
| 纯校验/规范化/分析 | 是 | 独立正确性测试、单一业务实现；不建领域类层次。 |
| 一个具体仓库 | 是 | 集中 SQLite 事务和一致读取，无通用仓库或 ORM。 |
| Excel/CSV 读取函数 | 是 | 解码需求不同，无通用导入器或插件注册表。 |
| 少量边界/结果记录 | 是 | 跨消费者保留校验状态和结果身份，无结构框架。 |
| 两个报告渲染器 | 是 | 必需格式使用不同展示工具，无模板引擎或共享图表抽象。 |
| 一个可选摘要适配器 | 仅交付 AI 时 | 隔离网络/配置失败，简单可调用对象足够，无智能体/供应商框架。 |
| 确定性摘要函数 | 是，作为建议离线报告路径 | 使用已算字段，不设独立智能引擎。 |
| 标准配置/日志 | 是，少量函数 | 本地路径和安全诊断，无配置服务或可观测性平台。 |
| 全局缓存、报告存储、工作队列 | 否 | 当前同步有限范围不需要，测量后再提出例外。 |
| API 服务器、依赖注入容器、事件总线、ORM、微服务 | 否 | 增加配置和间接层，却不解决必需 MVP 问题。 |

架构作为可审阅设计已完成，不代表软件实现。业务实施前解决所属数据/KPI/UI/质量文档，对齐开放产品冲突并审阅 AGENTS V1，不自动开始这些阶段。
